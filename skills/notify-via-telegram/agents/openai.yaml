interface:
  display_name: "Telegram Task Notifications"
  short_description: "Global or project-specific Telegram task updates"
  default_prompt: "Use $notify-via-telegram to route updates for this long task to the configured global and project Telegram destinations."
