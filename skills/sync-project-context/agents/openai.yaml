interface:
  display_name: "Sync Project Context"
  short_description: "Sync private project context across devices"
  default_prompt: "Use $sync-project-context to save or restore a private project handoff through an approved local folder or connected Google Drive."
