interface:
  display_name: "Sync Project Context"
  short_description: "Sync project chats and portable requirements"
  default_prompt: "Use $sync-project-context to synchronize project chats and reconcile portable project rules, skills, plugins, and safe settings without duplicating Git-owned state."
