interface:
  display_name: "Sync Project Context"
  short_description: "Sync project chats and portable requirements"
  default_prompt: "Use $sync-project-context to synchronize project chats through the configured backend or connected Google Drive by default, without duplicating Git-owned state."
